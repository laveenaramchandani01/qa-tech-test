const add = (accumulator, currentValue) => accumulator + currentValue;
    var row_a = [23,50,63,90,10,30,155,23,18];
	var row_b = [133,60,23,92,6,7,168,16,19];
	var row_c = [30,43,29,10,50,40,99,51,12];
//this is a function to sum up the values of the indexes in the arrays
function sum(numbers) {
  return numbers.reduce(add, 0);
}
//passing an generic argument to be able to re use for all three arrays
function find_middle_index(row) {
  for (let index = 0; index < row.length; index++) {
  const left = sum(row.slice(0,index));
  const right = sum(row.slice(index+1,row.length));
  //console.log(left_a);
  if (left == right) {
   return index;
    //console.log(index);
      }
}
return null;
}
console.log(find_middle_index(row_a));
console.log(find_middle_index(row_b));
console.log(find_middle_index(row_c));
